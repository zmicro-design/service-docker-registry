version: 0.1

log:
  fields:
    service: registry

#storage:
#  cache:
#    layerinfo: inmemory
#  filesystem:
#    rootdirectory: /var/lib/registry

http:
  addr: :5000

proxy:
  remote: https://registry-1.docker.io

